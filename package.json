{
  "name": "sunwise-integration",
  "private": true,
  "scripts": {
    "setup": "npm --prefix SunWise-Backend run setup && npm --prefix SunWise install",
    "dev": "node scripts/dev.mjs",
    "dev:backend": "npm --prefix SunWise-Backend run dev",
    "test": "npm --prefix SunWise-Backend test"
  }
}
