{
  "name": "sunwise-integration",
  "private": true,
  "scripts": {
    "setup": "npm --prefix backend run setup && npm --prefix frontend install",
    "dev": "node scripts/dev.mjs",
    "dev:backend": "npm --prefix backend run dev",
    "test": "npm --prefix backend test"
  }
}
